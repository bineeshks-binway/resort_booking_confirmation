<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking Confirmation</title>
    <link
        href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Inter:wght@400;600&display=swap"
        rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #ffffff;
            color: #333;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }

        .header {
            background-color: #1A4D2E;
            color: #D4AF37;
            padding: 40px;
            text-align: center;
            position: relative;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }

        .logo {
            width: 80px;
            height: 80px;
            object-fit: contain;
            margin-bottom: 10px;
            background: white;
            border-radius: 50%;
            padding: 5px;
        }

        .resort-name {
            font-family: 'Playfair Display', serif;
            font-size: 32px;
            font-weight: 700;
            margin: 0;
            letter-spacing: 1px;
        }

        .resort-address {
            color: #fff;
            font-size: 14px;
            margin-top: 5px;
            opacity: 0.9;
        }

        .content {
            padding: 40px;
            max-width: 800px;
            margin: 0 auto;
        }

        .booking-id {
            text-align: right;
            color: #666;
            font-size: 14px;
            margin-bottom: 20px;
            border-bottom: 1px solid #eee;
            padding-bottom: 10px;
        }

        .room-section {
            margin-bottom: 30px;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        .room-image {
            width: 100%;
            height: 300px;
            object-fit: cover;
        }

        .room-details {
            background: #fcfcfc;
            padding: 20px;
            border-top: 4px solid #D4AF37;
        }

        .room-title {
            font-family: 'Playfair Display', serif;
            font-size: 24px;
            color: #1A4D2E;
            margin: 0 0 5px 0;
        }

        .grid-container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-top: 30px;
        }

        .info-box {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            border-left: 3px solid #1A4D2E;
        }

        .info-label {
            font-size: 12px;
            text-transform: uppercase;
            color: #666;
            margin-bottom: 5px;
            letter-spacing: 0.5px;
        }

        .info-value {
            font-size: 16px;
            font-weight: 600;
            color: #333;
        }

        .price-summary {
            margin-top: 30px;
            background: #1A4D2E;
            color: white;
            padding: 25px;
            border-radius: 8px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .price-label {
            font-size: 18px;
        }

        .total-price {
            font-family: 'Playfair Display', serif;
            font-size: 32px;
            color: #D4AF37;
        }

        .services-list {
            margin-top: 20px;
        }

        .service-item {
            display: inline-block;
            background: #f0fdf4;
            color: #1A4D2E;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 12px;
            margin-right: 5px;
            border: 1px solid #1A4D2E;
        }

        .footer-qr {
            margin-top: 40px;
            text-align: center;
            border-top: 1px solid #eee;
            padding-top: 30px;
        }

        .qr-code {
            width: 120px;
            height: 120px;
        }

        .footer-text {
            margin-top: 15px;
            font-size: 12px;
            color: #888;
        }

        .status-badge {
            display: inline-block;
            padding: 5px 10px;
            border-radius: 4px;
            font-size: 12px;
            font-weight: bold;
            text-transform: uppercase;
        }

        .status-paid {
            background: #dcfce7;
            color: #166534;
        }

        .status-pending {
            background: #fee2e2;
            color: #991b1b;
        }

        .page-break {
            page-break-before: always;
            margin-top: 40px;
        }

        @media print {
            .page-break {
                page-break-before: always;
                margin-top: 50px;
            }

            .room-section,
            .info-box,
            .services-list {
                page-break-inside: avoid;
            }
        }
    </style>
</head>

<body>
    <div class="header">
        <img src="<%= logoData %>" class="logo" alt="Logo">
        <h1 class="resort-name">Wayanad Green Valley Resort</h1>
        <p class="resort-address">Meppadi, Wayanad, Kerala - 673577</p>
    </div>

    <div class="content">
        <div class="booking-id">
            Booking ID: <strong>#<%= bookingId %></strong>
        </div>

        <div class="room-section">
            <img src="<%= roomImageData %>" class="room-image" alt="Room View">
            <div class="room-details">
                <h2 class="room-title">
                    <%= roomType %> Room
                </h2>
                <div style="font-size: 14px; color: #666;">
                    Enjoy your stay in our premium <%= roomType.toLowerCase() %> suite with nature views.
                </div>
            </div>
        </div>

        <div class="grid-container">
            <div class="info-box">
                <div class="info-label">Guest Details</div>
                <div class="info-value">
                    <%= guestName %>
                </div>
                <div style="font-size: 14px; margin-top: 5px; color: #555;">
                    <%= phoneNumber %>
                </div>
                <div style="font-size: 14px; margin-top: 3px; color: #555;">
                    <%= guests %> Guests
                </div>
            </div>
            <div class="info-box">
                <div class="info-label">Stay Duration</div>
                <div style="margin-bottom: 5px;">
                    <span style="font-size: 13px; color: #666;">Check-in:</span><br>
                    <span class="info-value">
                        <%= new Date(checkIn).toLocaleDateString() %>
                    </span>
                </div>
                <div>
                    <span style="font-size: 13px; color: #666;">Check-out:</span><br>
                    <span class="info-value">
                        <%= new Date(checkOut).toLocaleDateString() %>
                    </span>
                </div>
            </div>
        </div>

        <% if (extraServices && extraServices.length> 0) { %>
            <div class="services-list">
                <div class="info-label" style="margin-bottom: 10px;">Included Services</div>
                <% extraServices.forEach(function(service) { %>
                    <span class="service-item">
                        <%= service %>
                    </span>
                    <% }); %>
            </div>
            <% } %>

                <% if (specialNotes) { %>
                    <div style="margin-top: 20px; font-size: 14px; color: #555; font-style: italic;">
                        <strong>Note:</strong>
                        <%= specialNotes %>
                    </div>
                    <% } %>

                        <div class="page-break">
                            <div class="price-summary">
                                <div>
                                    <div class="price-label">Total Amount</div>
                                    <div style="margin-top: 5px;">
                                        <span
                                            class="status-badge <%= paymentStatus === 'Paid' ? 'status-paid' : 'status-pending' %>">
                                            <%= paymentStatus %>
                                        </span>
                                    </div>
                                </div>
                                <div class="total-price">₹ <%= parseInt(price).toLocaleString('en-IN') %>
                                </div>
                            </div>

                            <div class="footer-qr">
                                <img src="<%= qrCodeData %>" class="qr-code" alt="Location QR">
                                <p class="footer-text">Scan to find us on Google Maps</p>
                                <p class="footer-text">www.wayanadgreenvalley.com | +91 98765 43210</p>
                            </div>
                        </div>
    </div>
</body>

</html>