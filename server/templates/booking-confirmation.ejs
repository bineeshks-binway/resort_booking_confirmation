<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Booking Confirmation</title>

  <link
    href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Inter:wght@400;600&display=swap"
    rel="stylesheet">

  <style>
    body {
      font-family: 'Inter', sans-serif;
      margin: 0;
      padding: 0;
      background: #ffffff;
      color: #333;
      -webkit-print-color-adjust: exact;
      print-color-adjust: exact;
    }

    .header {
      background: #1A4D2E;
      color: #D4AF37;
      padding: 40px;
      text-align: center;
      border-bottom-left-radius: 30px;
      border-bottom-right-radius: 30px;
    }

    .logo {
      width: 80px;
      height: 80px;
      object-fit: contain;
      background: #ffffff;
      border-radius: 50%;
      padding: 6px;
      margin-bottom: 12px;
    }

    .resort-name {
      font-family: 'Playfair Display', serif;
      font-size: 32px;
      margin: 0;
    }

    .resort-address {
      font-size: 14px;
      color: #ffffff;
      margin-top: 6px;
      opacity: 0.9;
    }

    .content {
      padding: 40px;
      max-width: 800px;
      margin: auto;
    }

    .booking-id {
      text-align: right;
      font-size: 14px;
      color: #666;
      margin-bottom: 20px;
      border-bottom: 1px solid #eee;
      padding-bottom: 10px;
    }

    .room-section {
      margin-bottom: 30px;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
    }

    .room-image {
      width: 100%;
      height: 300px;
      object-fit: cover;
    }

    .room-details {
      background: #fcfcfc;
      padding: 20px;
      border-top: 4px solid #D4AF37;
    }

    .room-title {
      font-family: 'Playfair Display', serif;
      font-size: 24px;
      color: #1A4D2E;
      margin: 0 0 6px;
    }

    .grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
      margin-top: 30px;
    }

    .info-box {
      background: #f8f9fa;
      padding: 20px;
      border-radius: 8px;
      border-left: 3px solid #1A4D2E;
    }

    .info-label {
      font-size: 12px;
      text-transform: uppercase;
      color: #666;
      margin-bottom: 6px;
    }

    .info-value {
      font-size: 16px;
      font-weight: 600;
    }

    .services {
      margin-top: 20px;
    }

    .service-item {
      display: inline-block;
      background: #f0fdf4;
      border: 1px solid #1A4D2E;
      color: #1A4D2E;
      padding: 5px 14px;
      margin-right: 6px;
      border-radius: 18px;
      font-size: 12px;
      margin-bottom: 5px;
    }

    .page-spacer {
      page-break-before: always;
      height: 60px;
      display: block;
    }

    .price-summary {
      border-top: 1px solid transparent;
      /* Prevent margin collapse */
      background: #1A4D2E;
      color: #ffffff;
      padding: 25px 30px;
      border-radius: 8px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .price-left {
      display: flex;
      flex-direction: column;
      gap: 5px;
      align-items: flex-start;
    }

    .price-label {
      font-size: 14px;
      text-transform: uppercase;
      opacity: 0.9;
      letter-spacing: 0.5px;
    }

    .total-price {
      font-family: 'Playfair Display', serif;
      font-size: 32px;
      color: #D4AF37;
    }

    .status-badge {
      padding: 5px 10px;
      border-radius: 4px;
      font-size: 12px;
      font-weight: 700;
      text-transform: uppercase;
    }

    .paid {
      background: #dcfce7;
      color: #166534;
    }

    .pending {
      background: #fee2e2;
      color: #991b1b;
    }

    .footer {
      margin-top: 40px;
      text-align: center;
      border-top: 1px solid #eee;
      padding-top: 30px;
    }

    .qr {
      width: 120px;
      height: 120px;
    }

    .footer-text {
      font-size: 12px;
      color: #888;
      margin-top: 10px;
    }
  </style>
</head>

<body>

  <!-- HEADER -->
  <div class="header">
    <img src="<%= logoData %>" class="logo" alt="Resort Logo">
    <h1 class="resort-name">Wayanad Green Valley Resort</h1>
    <p class="resort-address">Meppadi, Wayanad, Kerala - 673577</p>
  </div>

  <div class="content">

    <div class="booking-id">
      Booking ID: <strong>#<%= bookingId %></strong>
    </div>

    <!-- ROOM -->
    <div class="room-section">
      <img src="<%= roomImageData %>" class="room-image" alt="Room image">
      <div class="room-details">
        <h2 class="room-title">
          <%= roomType %> Room
        </h2>
        <p style="font-size:14px;color:#666;">
          Enjoy your stay in our premium <%= roomType.toLowerCase() %> suite with nature views.
        </p>
      </div>
    </div>

    <!-- DETAILS -->
    <div class="grid">
      <div class="info-box">
        <div class="info-label">Guest</div>
        <div class="info-value">
          <%= guestName %>
        </div>
        <div style="font-size:14px;color:#555;margin-top:4px;">
          <%= phoneNumber %>
        </div>
        <div style="font-size:14px;color:#555;">
          <%= guests %> Guests
        </div>
      </div>

      <div class="info-box">
        <div class="info-label">Stay</div>
        <div style="font-size:14px;color:#666;">Check-in</div>
        <div class="info-value">
          <%= new Date(checkIn).toLocaleDateString() %>
        </div>
        <div style="font-size:14px;color:#666;margin-top:6px;">Check-out</div>
        <div class="info-value">
          <%= new Date(checkOut).toLocaleDateString() %>
        </div>
      </div>
    </div>

    <% if (extraServices && extraServices.length) { %>
      <div class="services">
        <div class="info-label">Included Services</div>
        <% extraServices.forEach(service=> { %>
          <span class="service-item">
            <%= service %>
          </span>
          <% }) %>
      </div>
      <% } %>

        <% if (specialNotes) { %>
          <p style="margin-top:20px;font-size:14px;color:#555;">
            <strong>Note:</strong>
            <%= specialNotes %>
          </p>
          <% } %>

            <!-- PRICE -->
            <div class="page-spacer"></div>
            <div class="price-summary">
              <div class="price-left">
                <div class="price-label">Total Amount</div>
                <span class="status-badge <%= paymentStatus === 'Paid' ? 'paid' : 'pending' %>">
                  <%= paymentStatus %>
                </span>
              </div>
              <div class="total-price">₹ <%= parseInt(price).toLocaleString('en-IN') %>
              </div>
            </div>

            <!-- FOOTER -->
            <div class="footer">
              <img src="<%= qrCodeData %>" class="qr">
              <div class="footer-text">Scan to find us on Google Maps</div>
              <div class="footer-text">www.wayanadgreenvalley.com | +91 98765 43210</div>
            </div>

  </div>
</body>

</html>